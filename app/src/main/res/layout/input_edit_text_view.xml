<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/input_edit_text_view_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <android.support.design.widget.TextInputLayout
        android:id="@+id/input_edit_text_view_layout"
        style="@style/TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/form_input_height"
        android:layout_gravity="center_vertical"
        android:orientation="horizontal"
        app:hintEnabled="false"
        app:hintTextAppearance="@style/FloatingLabel">

        <android.support.design.widget.TextInputEditText
            android:id="@+id/input_edit_text_view_edit"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
            android:background="@null"
            android:imeOptions="actionNext"
            android:inputType="textImeMultiLine"
            android:paddingBottom="17dp"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:saveEnabled="false"
            android:theme="@style/EditTextForm"
            tools:hint="TEST" />

    </android.support.design.widget.TextInputLayout>

    <ImageView
        android:id="@+id/input_edit_text_view_eye"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_gravity="right"
        android:padding="15dp"
        android:src="@drawable/ic_eye"
        />

</RelativeLayout>